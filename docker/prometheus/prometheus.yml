global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "prometheus"
    static_configs:
      - targets: ["localhost:9090"]

  # Add the model-serving service here once it exposes /metrics, e.g.:
  # - job_name: "cogload-serving"
  #   static_configs:
  #     - targets: ["serving:8000"]
