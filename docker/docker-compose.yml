services:
  mlflow:
    image: dhi.io/mlflow:3
    container_name: mlflow_container
    ports:
      - "5001:5001"
    volumes:
      - ./mlruns:/mlruns
    command: >
      server  --host 0.0.0.0  --port 5001  --backend-store-uri /mlruns  --default-artifact-root /mlruns
